# VCS
.git
.gitignore

# Local env & secrets (never bake into the image)
.env
*.env
!.env.example

# Build artifacts
bin/
tmp/
*.exe
coverage.out
coverage.html

# Runtime file storage (can grow large — keep out of build context)
uploads/
generated/

# Docker / compose
Dockerfile
.dockerignore
docker-compose*.yml

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Docs & scripts not needed at build time
README.md
scripts/
.air.toml
